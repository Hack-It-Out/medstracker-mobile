/* eslint-disable prettier/prettier */
import {View, Text} from 'react-native';
import React from 'react';

const AppBar = () => {
  return (
    <View>
      <Text className="text-black">AppBar</Text>
    </View>
  );
};

export default AppBar;
